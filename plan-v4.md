# forge v4 规划（用户 2026-09-14 10:40 定稿）

> 核心思路：不是"把每个框架都塞进来"，而是**提取每个框架最不可替代的那一块能力**，对应到内核的某个具体模块或接口上。
> 一句话原则：每个框架只吸收它最不可替代的那一块，其余全部丢给契约和适配层。内核越小，组合越灵活，越不容易在联合开发时互相污染。

现状起点：8 个贡献模块中 5/8 已入库（heartbeat / scheduler / replay / toolhost / curator），`compactor`（OpenCode）与 `teams`（WorkBuddy）待收。

---

## 🧬 进化与自主层

### Hermes（对应 curator，已入库）
- **吸收什么**：自进化规划器（Self-Evolution Planner）+ 外部记忆持久化。让 Agent 空闲时基于遥测数据生成自我改进提案。
- **落地形态**：curator 现在只管策展；v4 给它加一个 **proposal 子接口** —— 规划器产出"提案对象"，必须经过闭环验证和 A/B 测试后才落地。
- **关键约束**：自我修改必须**冻结函数签名 + 全量测试**，防止越改越崩。

### OpenClaw（对应 heartbeat，已入库）
- **吸收什么**：心跳循环 + 主动触发。不是被动等指令，而是定期醒来读 `HEARTBEAT.md` 决定要不要发起工作。
- **落地形态**：heartbeat 现在是参考实现；v4 要把**事件触发**与**定时触发**做成两种策略并存，并接入 scheduler 统一调度。

---

## 👥 执行与特化层

### Claude Code（对应 toolhost，已入库，但只吸收了一半）
- **吸收什么**：子代理特化 + 独立上下文隔离。主 Agent 调度，子代理拥有独立上下文、受限工具集和专属模型，避免互相污染。
- **落地形态**：v4 在 toolhost 之上加一层 **subagent 抽象**。每个子代理是独立进程/会话，只暴露契约里声明的工具，结束后只回传结构化结果，不把中间上下文倒灌回主流程。

### Codex（对应闭环验证，散落在多个模块）
- **吸收什么**：评审-修复-验证（Review-Fix-Verify）硬门禁。每轮迭代必须产出记录文件，上一轮的测试报错作为下一轮输入，不通过就循环。
- **落地形态**：v4 抽成独立的 **gate 模块** —— 所有代码类任务必须过这个门禁，门禁规则（跑哪些测试、什么算通过）由契约配置，不允许 Agent 自己绕过去。

### OpenCode（对应 compactor，目前卡住）
- **吸收什么**：模型无关层（LLM Adapter）+ Plan/Build 双模式。
- **落地形态**：compactor 收完后，v4 把"模型选择权"从各 Agent 里抽出，统一成 **Adapter 层**。Plan 模式只读规划、产出 diff；Build 模式才允许写入。正好治 teams CLI 卡住的问题 —— WorkBuddy 的交互式 CLI 适合 Plan 模式，无头调用走 Build 模式。

---

## 🧩 极简核心与扩展层

### Pi Agent
- **吸收什么**：最小内核 + TypeScript 扩展 + 热重载（`/reload`）。
- **落地形态**：内核（纯标准库 Python ≥3.10）只保留**读/写/编辑/执行四个基础工具**，其余全部作为可插拔扩展；扩展支持热重载，改完即生效，不用重启整个 forge。

### dsh
- **吸收什么**：一切皆插件（微内核）+ 预制模式（标准编码 / 极简评测 / 插件自省）。
- **落地形态**：与 Pi Agent 的扩展系统合并，但 **dsh 的"预制模式"单独保留** —— 不同任务用不同模块组合与权限配置（评测模式只读、编码模式可写、自省模式只分析自身日志）。

---

## 🔌 生态与运行层

### StepClaw（对应 scheduler，已入库）
- **吸收什么**：高频轻量同步 + 中文工具调用。状态同步不需要高推理，Flash 级模型就够。
- **落地形态**：scheduler 现管调度；v4 给它加 **文件锁 + 变更日志协议** —— 任何 Agent 写文件前申请锁，写完释放并广播变更，其他 Agent 收到广播刷新本地上下文。这是多 Agent 并行的核心保险。

### zcode（对应 replay，已入库）
- **吸收什么**：仓库级大上下文阅读 + 轨迹回放。
- **落地形态**：replay 现做回放；v4 把**只读日志全链路记录**做成默认行为 —— 所有会话可断点续跑、可审计。

### TRAE（对应 toolhost 的适配层）
- **吸收什么**：前端/组件生成 + 实时建议面板。
- **落地形态**：**保持适配层，不强行并入内核**。TRAE 的强项是 UI 迭代，让它在自己领域跑，通过契约回传产物即可。

### WorkBuddy / CodeBuddy（对应 teams，待收）
- **吸收什么**：中文调度 + 文档生成 + 多 sub-agent 编排。
- **落地形态**：teams 收完后，定位成**任务路由层** —— 不参与核心逻辑，只负责把中文任务拆解、分发给对应 Agent、汇总结果。它读系统代理的坑已通过 7890 修好，v4 要确保它走**无头调用**。

---

## ⚖️ 重构优先级（三步走）

1. **先收尾**：等 `compactor` 与 `teams` 入库，8/8 齐了跑全量契约测试。
2. **再抽象**：把 **subagent / gate / LLM Adapter** 三个抽象层从现有模块里抽出来，作为 v4 的核心接口。
3. **后收敛**：内核瘦身到四个基础工具 + 扩展系统，其余全部插件化，支持热重载和预制模式。

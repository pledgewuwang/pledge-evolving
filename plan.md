# forge 路线图 · 10 天 / 30 亿 tokens → 初步交付品；6 个月 / 150 亿 → 半部工程品

## 0. 先把两个目标定义清楚（否则没法验收）

**初步交付品（10 天）**：一个**别人拿到能装、能跑、能看懂失败原因**的东西。

验收清单：
- [ ] 一条命令安装（`pip install -e .` 或等价），装完 `forge --version` 有输出 —— 本地启动器已验，pip 安装待测
- [x] `forge smoke` 能对着真实模型跑完一个真任务（读文件 → 改文件 → 自检通过）—— 2026-09-15 达成：deepseek-v4-flash 真模型 S1–S5 全绿，并因此挖出两个只有真端点能暴露的缺陷（schema 简写不归一、多 tool_calls 无应答）
- [ ] 至少 3 个真实任务端到端成功，且过程可从事件日志复盘 —— 进行中（已接进 30 分钟循环，每轮必跑）
- [ ] 失败可自诊断：`forge doctor` 覆盖环境、配置、密钥、网关、代理、贡献模块六类问题
- [x] 文档齐：README（怎么用）、CONTRACT（怎么写模块）、PLAN（这份）、INDEX（模块清单）
- [ ] 自检 ≥ 260 项，全绿；贡献模块 ≥ 12 份 —— 当前 241 项全绿，11 份（9 健康 + 2 待补证据）

**半部工程品（6 个月）**：只差真实用户规模化实测的成品。缺的不是功能，是**别人拿去用的那部分证据**（压测数据、兼容矩阵、迁移演练）。

## 1. 现状基线（本次迭代结束时）

| 项 | 值 |
| --- | --- |
| 核心模块 | 19 个文件，零第三方依赖 |
| 离线自检 | **228 项全绿** |
| 贡献模块 | **11 份入库**（阶跃龙虾 / zcode ×2 / Trae ×2 / Hermes ×2 / WorkBuddy / OpenCode / DSH / 参考实现） |
| 作者自带断言 | 164 条 |
| 能力标签 | 30 个，钩子可达性缺陷 0 |
| 模型供给 | DeepSeek 主力 + MiMo 备用 + Claude 审查，MoA 面板已开 |
| 环境 | 网关常驻（8810/8811），协议翻译已验 |

## 2. 关键判断：30 亿 tokens 该花在哪

这是这份计划里最重要的一条：

<div class="rich-panel"><div class="rich-panel-title">不要把预算花在"继续堆模块"上</div><div class="rich-muted">写一个模块的成本低得可以忽略（本次 11 份模块的作者侧开销合计不到一顿饭）。<b>真正吃掉 30 亿的是"写→跑→炸→修"的闭环轮次</b>：让 agent 对着真实代码、真实模型、真实失败反复迭代，每一轮都要把上下文重新读一遍。所以计划按<b>闭环轮次</b>排，不按模块数排。</div></div>

单轮端到端闭环的成本结构（实测口径）：

| 环节 | 单轮量级 | 说明 |
| --- | --- | --- |
| 读上下文 + 规划 | 50–150 万 tokens | 代码库 + 契约 + 上一轮失败记录 |
| 实际改动 | 100–300 万 | 写代码、改配置、写测试 |
| 跑测试与定位失败 | 100–400 万 | 失败定位是最贵的一步，往往要读大量日志 |
| 审查与回归 | 50–150 万 | 交叉审查 + 全量自检 |
| **合计** | **300–1000 万 / 轮** | 取 500 万为中位估计 |

按 3 亿/天算：**每天 6 轮高密度闭环**，10 天 60 轮，合计约 **30 亿 —— 预算刚好对齐**，不是巧合，说明 30 亿对应的是"高强度跑 10 天"。

折算成钱：30 亿 tokens ×（MiMo ¥0.0754/M ～ DeepSeek ¥0.1595/M）≈ **¥225 – ¥480**。**钱不是约束，往返时间才是**。

## 3. 10 天计划（30 亿 tokens）

| 天 | 主线 | 出口条件 | 预算 |
| --- | --- | --- | --- |
| **D1–D2** | 闭环可用：打包 + `smoke` 命令 + 端到端冒烟 | 装完能跑通一个真任务 | 4 亿 |
| **D3–D5** | 真实任务闭环 ×15：拿 3 个真需求（文档处理 / 代码改造 / 数据整理）反复跑 | 三类任务各有稳定通过路径 | 9 亿 |
| **D6–D7** | 失败归因：把每一类失败变成 `doctor` 能报的项 | 失败可自诊断，不靠人肉猜 | 6 亿 |
| **D8–D9** | 贡献模块扩容 + 替换验证（12–15 份）；hooks 运行时从 module 接线进主循环 | 能力标签 ≥ 40，接线跑通 | 6 亿 |
| **D10** | 交付品化：安装文档、示例、退出标准逐条核验 | 验收清单全绿 | 5 亿 |

预算分配：**闭环实测 60% / 集成修复 20% / 模块与接线 12% / 文档与审查 8%**。

## 4. 6 个月计划（150 亿 tokens）

| 阶段 | 目标 | 出口条件 |
| --- | --- | --- |
| **M1–M2** | 工程骨架 | 打包发布流程、CI（Windows/Linux 双平台）、凭证管理（密钥不落盘明文）、内核级沙箱、可观测面板 |
| **M3–M4** | 规模化能力 | 多会话并发、插件市场与签名、性能基准（每轮 P50/P95 延迟）、MCP 真连接 |
| **M5–M6** | 稳定性与运维 | 版本迁移演练、备份与恢复、诊断工具链、文档站；**做到"只差用户实测"** |

平均 8000 万 tokens/天，约 16 轮闭环/天。折算成本约 ¥1100 – ¥2400（按当前费率）——**仍然不是钱的问题**。

## 5. 这份计划里最可能翻车的三处

1. **往返时间**：每轮闭环要我这一侧的上下文把状态重新装回来。今天的实测已经暴露过——我这边一断线，整条流水线就停 4 小时。对策：**关键进程常驻化**（网关看门狗已具备条件）、把"待办派单"做成可续跑的队列而不是每轮重建。
2. **贡献者的稳定性**：opencode 三次跑挂、Trae 交付带 BOM、WorkBuddy 被代理卡死。对策：保留**多实现**（同一能力允许多份模块），失败时换实现而不是停工。
3. **验证标准漂移**：自检项是我写的，可能自己骗自己（本轮就出现过我的隐藏用例写错、把 14/14 的模型判成 11/14）。对策：**每个里程碑都要有独立于作者的验证**，且验证用例公开可复跑。

## 6. 马上要做的三件事（下一轮）

1. `pyproject.toml` + 入口点：让 `pip install -e .` 和 `forge` 命令成立（D1 的主线）
2. `forge smoke`：一条命令跑通"读文件 → 改文件 → 自检通过"，用真模型
3. 把审查发现的 5 条缺陷回传作者（`replay` 的 `until_ordinal` 类型守卫、`scheduler` 的无用 `import time`、`toolhost` 的跨平台路径用例、`heartbeat` 能力命重、`fork_plan` 类型守卫）——我不替它们改，但每条都写清了位置、触发条件和修法

审查已完成并输出（仅写了一份审查报告到 plan 暂存文件，未改动任何项目代码）。

**三个最关键的判断：**

1. **`model.primary` 是死配置** —— `model.py:199` 读出来后 `cls(...)`(202-208) 根本没这个形参，`loop.py:238` 也不传。base.json 里"主力档 deepseek"一个请求都发不出去，实际全走两个本地环回网关。0.5h 可修。

2. **上一轮 22 条 P0/P1 基本没修** —— 已修 1 条（子代理 registry clone）、部分修 3 条（eval AST 白名单、注入正则、session 双通道）、**仍在 18 条**。reports/10 的 15 条 contrib 问题全在，包括三个 P0（toolhost 命名即权限、MCP 前缀互斥、legacy 测试合成假 PASS）。

3. **"最小内核"名不副实** —— `build_agent` 无条件构造 5 个子系统；压缩策略在 `memory.py` 和 `contrib/compactor.py` 各实现一份且规则不同；`loop.py` 完全不认识 `federation/evolution/pricing`，readme 宣传的自我进化与联邦实际没接进主循环。

如果需要我按报告动手修，请告诉我从第几项开始（建议从优化 1 开始，成本最低、收益最大）。

---

## 本轮已落地的优化（集成者，2026-09-14 21:0x）

### 优化 1：`model.primary` 死配置 —— 已修并验证

- **位置**：`forge/model.py`（`ModelRouter.__init__` / `from_config` / `_order`）。
- **问题**：`from_config` 读出了 `cfg.get("model", "primary")`（bundles/base.json → `["deepseek","deepseek-flash"]`），但构造 `cls(...)` 时既没有 `primary` 形参、也没传值 → `_order()` 的首选永远是空，实际只会走 `fallback` 链（两个本地环回网关），配置里的主力档一个请求都发不出去。
- **修法**（向后兼容）：
  1. `__init__` 新增 `primary: tuple[str, str] | None = None`，存 `self.primary`；
  2. `from_config` 传入 `primary=tuple(primary) if primary else None`；
  3. `_order` 开头 `primary = primary or self.primary`（调用方显式指定仍优先）。
- **验收**：
  - `python run.py selftest` → **228/228 通过**（无回归）；
  - 直接构造验证：`_order(None)` → `[('deepseek','deepseek-flash'), ('mimo','claude-sonnet-5')]`（修复前只有 mimo）；未配置 primary 的路由器行为不变。

### 待办（未动，需确认后再改）

- 上一轮 18 条仍在的 P0/P1，尤其三条 P0：`toolhost` 命名即权限、`mcp_bridge` 前缀与 toolhost 正则互斥、`registry` legacy 测试合成假 PASS。
  - 前两条是**跨模块契约**问题，改 `authorize` 签名/工具命名规范会触碰冻结契约，建议作为 v4 的接口变更统一处理，而不是在 v3 里偷偷改语义；
  - 第三条会让 toolhost / mcp_bridge **当场被隔离**（它们目前只靠合成 PASS 入库），必须同时给这两份补上真断言，否则会把 9/9 healthy 打成 7/9。

